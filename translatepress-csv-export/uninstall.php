<?php
/**
 * Uninstall handler. The plugin stores no options or tables of its own,
 * so there is nothing to clean up — TranslatePress's own data is left
 * untouched.
 */
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
